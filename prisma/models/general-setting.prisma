model GeneralSetting {
  id                         String  @id @default(ulid())
  platformName               String?
  supportEmail               String?
  PlatformDescription        String?
  isAutoApproveGarages       Boolean @default(false)
  isAutoApproveParts         Boolean @default(false)
  isEmailNotificationEnabled Boolean @default(true)
}
